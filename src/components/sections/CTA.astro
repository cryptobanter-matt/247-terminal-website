---

---

<section class="section relative overflow-hidden">
  <div class="container">
    <div class="relative rounded-3xl overflow-hidden">
      {/* Background */}
      <div class="absolute inset-0 bg-gradient-to-br from-accent/20 via-bg-surface to-bg-surface" />
      <div class="absolute inset-0 bg-[radial-gradient(ellipse_at_top_right,_var(--color-accent-glow-strong),_transparent_50%)]" />

      {/* Content */}
      <div class="relative z-10 px-4 sm:px-6 py-12 sm:py-16 lg:px-16 lg:py-24 text-center">
        <h2 class="text-2xl sm:text-3xl md:text-4xl lg:text-5xl font-bold mb-6 max-w-3xl mx-auto">
          While You're Reading This,
          <br />
          <span class="text-accent text-glow">Someone Just Caught A 150% Move</span>
        </h2>

        <p class="text-text-secondary text-base sm:text-lg mb-8 max-w-2xl mx-auto">
          Stop missing out on market-moving news. Join thousands of traders who are already ahead of the curve.
        </p>

        <div class="flex flex-col sm:flex-row gap-3 sm:gap-4 justify-center">
          <a
            href="#pricing"
            class="btn btn-primary text-base sm:text-lg px-6 sm:px-8 py-3 sm:py-4 animate-pulse-glow"
          >
            Get Access Now
            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7l5 5m0 0l-5 5m5-5H6" />
            </svg>
          </a>
          <a
            href="https://discord.com/invite/uBrhYNDEpY"
            target="_blank"
            rel="noopener noreferrer"
            class="btn btn-secondary text-base sm:text-lg px-6 sm:px-8 py-3 sm:py-4"
          >
            Join Discord
          </a>
        </div>

        {/* Trust indicators */}
        <div class="mt-12 flex flex-wrap items-center justify-center gap-6 text-text-tertiary text-sm">
          <span class="flex items-center gap-2">
            <svg class="w-4 h-4 text-success" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" />
            </svg>
            No credit card for trial
          </span>
          <span class="flex items-center gap-2">
            <svg class="w-4 h-4 text-success" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" />
            </svg>
            Cancel anytime
          </span>
          <span class="flex items-center gap-2">
            <svg class="w-4 h-4 text-success" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" />
            </svg>
            24/7 Support
          </span>
        </div>
      </div>
    </div>
  </div>
</section>
